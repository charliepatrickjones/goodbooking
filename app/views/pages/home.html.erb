<div class="banner">
  <div>
    <h1 class="text-center">Find a reservation</h1>
    <div class="row-12 justify-content-center">
      <%= render "shared/searchbar" %>
    </div>
  </div>
</div>

<div class="container justify-content-center">
  <h3>Top rated</h3>
  <div class="row rest-carousel">
    <% @restaurants.each do |restaurant|%>
      <div class="card-trip">
        <% if restaurant.photos.attached? %>
          <%= cl_image_tag restaurant.photos.first.key %>
        <% else %>
          <img class="rounded" src="https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/skateboard.jpg" />
        <% end %>
        <div class="card-trip-info mt-2">
          <h2 class="mb-2"><%= link_to "#{restaurant.name} (#{restaurant.category}) ",  restaurant_path(restaurant)%></h2>
          <p class="mb-2"><%= restaurant.address %></p>
          <h2 class="card-restaurant-rating text-center">
            <% rating_int = restaurant.rating.floor %>
            <% rating_dec = restaurant.rating - rating_int %>
            <% if rating_dec >= 0.75 %>
              <%= content_tag(:i, nil, class: "fas fa-star") * (rating_int + 1) %>
            <% elsif rating_dec >= 0.25 %>
              <%= content_tag(:i, nil, class: "fas fa-star") * rating_int %>
              <%= content_tag(:i, nil, class: "fas fa-star-half-alt")%>
            <% else %>
              <%= content_tag(:i, nil, class: "fas fa-star") * rating_int %>
            <% end %>
          </h2>
        </div>
      </div>
    <% end %>
  </div>
  <div class="row justify-content-center">
    <h3>Why Good Booking?</h3>
    <div class="d-flex">
      <div class="col-6">
        <div>
          <h3>Save time</h3>
          <p>You are a busy restaurant owner, and dont have time to allow bad bookings into your restaurant. Booking with us saves you, your staff and your customer valuable time.</p>
        </div>
      </div>
       <div class="col-6">
        <div>
          <h3>Rate and Review</h3>
          <p>We provide you with a quick,clear and easy way to leave reviews and ratings on your experience. Whether that be as a guest reviewing a restaurant, or a restaurant reviewing a guest.</p>
        </div>
      </div>
    </div>
    <div class="d-flex">
      <div class="col-6">
        <div>
          <h3>Take control</h3>
          <p>We want to put control back in the restaurant owners hands. Allowing you to carefully consider your guests dining history, punctuality to a reservation and past reviews, ensuring its a good match.</p>
        </div>
      </div>
       <div class="col-6">
        <div>
          <h3>Join our community</h3>
          <p>Be part of a burgeoning foodie community that are improving the restaurant industry whilst being on the same mission, to be the best booking.</p>
        </div>
      </div>
    </div>

  </div>
</div>
