<div class="container">  
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :booking_rating, as: :select, collection: 1..5 %> 
    <%= f.input :content %>
    <%= f.button :submit %>
  <% end %>
<div>